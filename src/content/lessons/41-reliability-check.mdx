---
title: "Cost, Latency & Reliability Check"
moduleId: "reliability"
order: 4
type: "quiz"
summary: "Quick knowledge check on budgets, caching, and reliability patterns."
estMinutes: 4
questions:
  - prompt: "According to the lessons, where does budget enforcement live?"
    options:
      - "The model stops itself when its internal token counter reaches the limit"
      - "The provider API enforces the budget and returns a quota error"
      - "The budget is counted across all calls including cache hits"
      - "The agent wrapper enforces the budget in code, stopping the run when exhausted"
    answerIndex: 3
    explanation: "Lesson 38 states that a budget is a safety property and that every agent run carries an explicit budget enforced by the wrapping code. The model has no knowledge of the run-level budget; it is the agent code that stops the loop and returns a partial result."
  - prompt: "In the Budgeted wrapper from the interactive lesson, what is the cache key?"
    options:
      - "The exact input prompt"
      - "The call counter value at the time of the request"
      - "A hash of the model's previous output"
      - "The step number in the outer agent loop"
    answerIndex: 0
    explanation: "Lesson 39 shows `if prompt in self.cache` as the lookup and stores the result under `self.cache[prompt]` -- the key is the exact input prompt. The call counter tracks budget consumption, not cache identity, and the cache stores inputs-to-outputs, not outputs-to-outputs."
  - prompt: "In the Budgeted wrapper, which check happens first on every call?"
    options:
      - "The budget check -- reject if calls >= budget before touching the cache"
      - "The cache check -- return cached result if the prompt was seen before"
      - "The model call -- then cache the result and increment the counter"
      - "A jitter delay -- then the cache check, then the budget check"
    answerIndex: 1
    explanation: "Lesson 39 checks `if prompt in self.cache` before `if self.calls >= self.budget`. This ordering means a cached hit is returned without consuming any budget. Checking the budget first would incorrectly block a request that could have been served for free from cache."
  - prompt: "Why does Lesson 40 say retrying a write is unsafe without idempotency?"
    options:
      - "Reads are also non-idempotent and must use unique IDs to be safe"
      - "Retries are safe by default because the retry cap bounds total calls"
      - "A timeout that triggers a retry can execute the action twice without idempotency"
      - "Idempotency is only required when switching to a fallback model, not for retries"
    answerIndex: 2
    explanation: "Lesson 40 states that without idempotency a timeout that triggers a retry can execute an action twice. The fix is to assign a unique identifier to each action so the receiver can de-duplicate. Reads are typically idempotent by contrast; it is writes and actions that require deliberate design."
  - prompt: "What is the purpose of adding jitter to exponential backoff?"
    options:
      - "Jitter increases the effective retry cap so the agent can attempt more calls"
      - "Jitter prevents concurrent agents from retrying in lockstep"
      - "Jitter satisfies the idempotency requirement for retried writes"
      - "Jitter resets the circuit breaker after a burst of failures"
    answerIndex: 1
    explanation: "Lesson 40 states that a small random offset (jitter) prevents concurrent agents from retrying in lockstep. Exponential backoff spaces retries to reduce load on a recovering service; jitter staggers simultaneous retries from multiple agents. Idempotency and circuit breaking are separate concerns addressed elsewhere in the same lesson."
---

A knowledge check for the Cost, Latency & Reliability module. Answer every question,
then submit to see your score and an explanation for each. Score 70%
or higher to mark this lesson complete; you can retry as many times
as you like.
