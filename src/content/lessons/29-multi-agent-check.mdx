---
title: "Multi-Agent Systems Check"
moduleId: "multi-agent"
order: 4
type: "quiz"
summary: "Quick knowledge check on supervisor/worker routing and coordination."
estMinutes: 4
questions:
  - prompt: "When should you reach for a multi-agent architecture instead of a single agent with tools?"
    options:
      - "Whenever parallel execution would reduce task latency"
      - "Only when roles are genuinely distinct and cannot share a single instruction without becoming incoherent"
      - "Whenever the task requires more than three sequential tool calls"
      - "When multiple agents need to write to the same shared resource"
    answerIndex: 1
    explanation: "Lesson 26 states: reach for multi-agent only when roles are genuinely distinct and cannot share a single instruction without becoming incoherent. Parallelism and multi-step tool use are not sufficient reasons -- the coordination overhead must be justified by genuine role separation, not just speed or call count."
  - prompt: "In the supervisor/worker pattern, what is the supervisor's responsibility?"
    options:
      - "Executing the work of each subtask directly to keep latency low"
      - "Sharing a single instruction set with all workers to avoid duplication"
      - "Writing all results to shared state so workers can read each other's output"
      - "Routing subtasks to workers whose roles match and assembling the results"
    answerIndex: 3
    explanation: "Lessons 26 and 27 are explicit: the supervisor routes each subtask to the worker whose role matches and assembles results -- it never does the work itself. Having the supervisor execute work directly would collapse the role separation that justifies the pattern."
  - prompt: "What makes a handoff well-designed according to the lessons?"
    options:
      - "It passes the entire conversation history so the worker has full context"
      - "It allows the worker to re-route back to the supervisor if the task is ambiguous"
      - "It batches several subtasks into one message to reduce model calls"
      - "It provides a short context plus a clear, narrow, self-contained instruction"
    answerIndex: 3
    explanation: "Lesson 26 defines a well-designed handoff as a short context plus a clear, narrow instruction that is self-contained -- the receiving agent should not need to re-read the entire conversation history. Passing the full history or allowing free re-routing back are specifically the patterns that cause poorly scoped handoffs and loops."
  - prompt: "How should cost budgeting work in a multi-agent system?"
    options:
      - "Each worker tracks its own step count and halts independently when its budget is exhausted"
      - "Cost is the same as for a single agent because workers share the supervisor's context"
      - "Budget total steps across the whole team, not per agent, because cost compounds across agents"
      - "Cost only multiplies when workers spawn additional sub-workers beyond the first level"
    answerIndex: 2
    explanation: "Lesson 28 states: budget total steps across the whole team, not per agent -- multi-agent cost compounds. A two-worker system with a supervisor costs at least three model calls for the same task a single agent handles in one pass, so per-agent budgets systematically undercount real cost."
  - prompt: "When does Lesson 28 recommend using a tool instead of a dedicated worker agent?"
    options:
      - "When the subtask is a single model call with a well-defined schema"
      - "When the subtask needs multiple steps and its own memory across those steps"
      - "When the supervisor must route the same subtask to more than one worker"
      - "When independent deployability of the subtask component is required"
    answerIndex: 0
    explanation: "Lesson 28 draws a direct line: use a tool when the subtask is a single call with a well-defined schema -- no separate agent identity, no handoff protocol. Use a worker when the subtask needs its own agent loop -- multiple steps, its own tool set, its own memory. The cases for choosing a worker (multi-step loop, own memory, independent deployability) are the correct reasons to add an agent."
---

A knowledge check for the Multi-Agent Systems module. Answer every question,
then submit to see your score and an explanation for each. Score 70%
or higher to mark this lesson complete; you can retry as many times
as you like.
